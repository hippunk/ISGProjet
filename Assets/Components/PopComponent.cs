using UnityEngine;

public class PopComponent : MonoBehaviour {
	// Advice: FYFY component aims to contain only public members (according to Entity-Component-System paradigm).
	public bool popMacro;
	public bool popT8;
	public bool popB;
}